<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ar.com.develup.desafioclublanacion" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/ClubLaNacionTheme" >
        <activity
            android:name=".actividades.ActividadPrincipal"
            android:label="@string/title_activity_actividad_principal" >

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".actividades.ActividadConfiguracionInicial"
            android:screenOrientation="portrait"/>
    </application>

</manifest>
